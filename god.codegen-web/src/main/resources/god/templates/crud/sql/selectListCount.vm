/*
A, B, C
L, M, N
X, Y, Z
*/
/* select${entity.pcName}ListCount */
SELECT COUNT(*) TOTCNT
  FROM ${entity.name} A /* $!{entity.tableComments} */
 WHERE 1 = 1
#set($i=0)	
#foreach($attribute in $primaryKeys)
#if($i == 0)
--   AND A.${attribute.name} = ${attribute.values} /* $!{attribute.columnComments} */
#else
--   AND A.${attribute.name} = ${attribute.values} /* $!{attribute.columnComments} */
#end
#set($i=$i+1)
#end
;
